apiVersion: v1
kind: Service
metadata:
  name: {{ include "django-student-app.fullname" . }}
  labels:
    {{- include "django-student-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "django-student-app.selectorLabels" . | nindent 4 }}
